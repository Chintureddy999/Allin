Thymeleaf
--------------
it is way by which we can design dynamic GUI for web applications.

Thymeleaf is server side templete engine for web applications(we will create web pages using thymeleaf )

we need to create HTML pages(called as templeates in thymeleaf) and use thymeleaf tags in them

Steps
1)add dependency in pom.xml

<h3 th:text="${name}"></h3>

here th:text is thymeleaf attribute
